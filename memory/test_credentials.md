# Test Credentials — JRY OS Portfolio

## Studio (Admin) Panel
- Opened via the **Studio** app in the dock (gear icon).
- Passcode: `CANTRUSHGREATNESS`
- Set via backend `.env` key `ADMIN_PASSCODE`.
- Used to add/delete Work items and Network clients.

No user accounts / login system — single-owner content management via passcode only.
